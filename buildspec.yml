version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing dependencies..."
      - npm install -g yarn typescript
      - yarn install
  build:
    commands:
      - yarn build

 post_build:
    commands:
      - echo "Starting the project..."
      - yarn start